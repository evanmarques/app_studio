// Define um enumerador (enum) para representar os diferentes níveis de planos.
// Usar um enum previne erros de digitação e torna o código mais legível.
enum Plan {
  free,
  basic,
  advanced,
  premium,
}
